/**
 * Faceti un proiect de Spring, adaugati un controller si expuneti mai multe endpoint uri (ex GET, POST).
 * Utilizati o liste de elemente (ex product, student, book) cum am facut la curs.
 */

package ro.itschool.homework_project_0102;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class HomeworkProjectApplication {

	public static void main(String[] args) {
		SpringApplication.run(HomeworkProjectApplication.class, args);
	}

}
